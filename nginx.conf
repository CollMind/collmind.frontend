server {
    listen 8080;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}
```

---

## frontend/.dockerignore
```
node_modules
build
.git
.gitignore
*.md
.env
.env.*
```

---

## Bitbucket Pipeline'ı Güncelle

Frontend pipeline'ında `VITE_API_URL` yerine `REACT_APP_API_URL` kullanmamız lazım. Frontend reposuna şu variable'ı ekle:

**Repository settings → Pipelines → Repository variables**
```
REACT_APP_API_URL → (backend deploy olduktan sonra ekleyeceğiz)
Secured: ✅